---
title: Пульс веб-платформы 16.05.2025
description: Новости веб-платформы
pubDate: May 16 2025
heroImage: /blog-placeholder-pulse.jpg
tags:
  - Пульс_веб_платформы
---

### Новости

- в Chrome 136 [обезопасили хранение истории посещения ссылок](https://developer.chrome.com/blog/visited-links): с помощью безобидного селектора `:visited` с вредоносного сайта можно прочекать вашу историю посещения ссылок, так как она применяется к ссылкам сразу на всех сайтах; теперь же история посещения ссылок будет ограничена только конкретным сайтом, и со стороннего сайта вынуть из CSS её будет нельзя
- [Safari обновился до 18.5](https://webkit.org/blog/16923/webkit-features-in-safari-18-5/): добавили поддержку Declarative Web Push, который работает без Service Worker
- а в версии Safari Technology Preview [допилили функцию](https://webkit.org/blog/16929/contrast-color/) `contrast-color()`, которая для переданного цвета автоматом подбирает контрастный чёрный или белый:

```css
color: contrast-color(purple);
```

- в Edge [пробуют новое API ARIA Notify](https://blogs.windows.com/msedgedev/2025/05/05/creating-a-more-accessible-web-with-aria-notify/) с помощью которого можно программно вызвать произнесение сообщения для определённой DOM-ноды скринридером:

```js
document.querySelector("#text-editor").ariaNotify("Selected text is bold");
```

- [исходники Firefox иронично завезли](https://github.com/mozilla-firefox/firefox) на майкрософтовский Github, видимо уже готовятся сдавать в музэй
- оказывается для [popover-ов и anchor positioning](https://www.oddbird.net/2025/05/06/polyfill-updates/) есть прям «официальные» полифиллы [@oddbird/popover-polyfill](https://github.com/oddbird/popover-polyfill/tree/v0.6.0) и [@oddbird/css-anchor-positioning](https://github.com/oddbird/css-anchor-positioning), так что если вы не использовали их по причине нераспостранённости в браузерах, то это не проблема
- [вышел Parcel v2.15.0](https://parceljs.org/blog/v2-15-0):
  - движок переписан на Rust
  - внутри начали использовать другой HTML-парсер, [html5ever](https://github.com/servo/html5ever) из движка Servo
  - с SVGO перешли на Rust-аналог — [OXVG](https://github.com/noahbald/oxvg) (работает [в разы быстрее](https://github.com/noahbald/oxvg/wiki/Benchmarks), чем JS; хотя уход с JS-решений, как по мне, в долгую ухудшает поддерживаемость тулов в угоду сиюминутному ускорению)

### Проекты

- [chrono](https://github.com/wanasit/chrono) — парсер даты из строки на человеческом языке в машиночитаемый формат

```js
chrono.parseDate("An appointment on Sep 12-13");
// Fri Sep 12 2014 12:00:00 GMT-0500 (CDT)
```

- [void](https://github.com/voideditor/void) — опенсорсный аналог Cursor (на самом деле ещё один пока ещё бесплатный форк VS Code)
- [svg-to-css](https://css-generators.com/svg-to-css/) — транслятор нечитаемой SVG-формы в более читаемый CSS-синтаксис `clip-path: shape()`
- [leptos](https://leptos.dev) — React-подобный фреймворк на Rust со знакомыми паттернами (реактивные сигналы, подобие JSX, компоненты)
- [basecoatui](https://basecoatui.com/) — shadcn/ui, портированный с React в чистый HTML/CSS
- [eslint-plugin-react-you-might-not-need-an-effect](https://github.com/NickvanDyke/eslint-plugin-react-you-might-not-need-an-effect) — плагин для ESLint, подсвечивающий [лишнее использование `useEffect`](https://react.dev/learn/you-might-not-need-an-effect)

### Статьи, мнения, туториалы

#### JS/TS

- эволюция альтернативного минималистичного генератора статических сайтов [Nue JS](https://github.com/nuejs/nue/tree/master/packages/nuejs) привела к [ребредингу в Hyper](https://nuejs.org/blog/introducing-hyper/) — также минималистичный, но уже полноценный headless view layer, которым изначально и планировался React, но потом что-то пошло не так
- если вы не уверены точно, какой будет лог у следующего кода, то есть [подробное объяснение о работе промисов](https://www.deepintodev.com/blog/how-promises-work-in-javascript):

```js
new Promise((resolve) => {
  console.log("Deep");
  resolve("12");
}).then((result) => console.log(result));

console.log(25);
```

- если ваш проект с JS/TS, JSX/TSX, CSS, у вас нет желания мигрировать на новый ESLint (или же надоело дружить между собой Pretier и ESLint), зато есть желание куда-то съехать, то вот [гайдик по переезду на Biome](https://blog.appsignal.com/2025/05/07/migrating-a-javascript-project-from-prettier-and-eslint-to-biomejs.html)
- противоречие Rust- и JS-тулингов [даёт о себе знать](https://github.com/oxc-project/oxc/issues/10048): React Compiler — JS-тула, использует Babel, и не работает с Rust-тулченами типа SWC и OXC
- пример [реализации простой Dependency Inversion в React](https://cekrem.github.io/posts/dependency-inversion-in-react/), когда отделяется слой работы с данными и подтягивается в компонент как зависимость
- Дэн не унимается и [продолжает толкать свою телегу](https://overreacted.io/static-as-a-server/): на этот раз напоминание, что RSC могут рендериться в билд-тайме в статичный HTML вовсе без использования «серверных» функций
- [тонкость работы с контекстом в React](https://julesblom.com/writing/parent-owners-context): если вынести контекст-провайдер в отдельную обёртку и прокидывать содержимое в него через `children`, то при изменении стейта внутри этой обёртки можно избавиться от лишних ререндеров `children`, так как они не меняются (и [ещё одна статейка](https://blacksheepcode.com/posts/no_react_context_is_not_causing_too_many_renders) про этот же подход, но с более обширным объяснением)
- [новая книга по основам TS «Total TypeScript Essentials»](https://www.totaltypescript.com/books/total-typescript-essentials) от Matt Pocock: пока не читал, но одобряю не глядя

#### CSS

- как с помощью CSS-анимаций делать [простую «ретро»-мультипликацию](https://www.smashingmagazine.com/2025/05/smashing-animations-part-1-classic-cartoons-inspire-css/): когда `@keyframes` используются буквально по прямому назначению, декорации движутся, показываются и скрываются, а в итоге всё кажется живым
- реализация [схем гитарных аккордов](https://dev.to/madsstoumann/guitar-chords-in-css-3hk8) на гридах, фукнции `attr()`, градиентах и container-queries (Chrome only)
- так, тут [опасная и экспериментальная CSS-магия](https://css-tricks.com/scroll-driven-animations-inside-a-css-carousel/): берём scroll animations, добавляем новенькие CSS-карусели и вишенкой на торте — scroll snaps: так можно в карусельке реализовать динамическую высоту слайдов в зависимости от скролла (Chrome only)
- псевдоэлементом `::first-letter` можно стилизовать не только обычную букву, но и любой символ, [к примеру, emoji](https://frontendmasters.com/blog/initial-letter-with-emoji/):

```html
<li>🍎 <strong>Apple</strong> Text</li>
```

```css
li::first-letter {
  initial-letter: 3;
}
```

- тема `height: 100%` [неочевидно работает в CSS](https://www.joshwcomeau.com/css/height-enigma/) из-за двух противоречащих друг другу механизмов: 1) по умолчанию во Flow layout родитель принимает минимально необходимую высоту для показа дочерних элементов; 2) высота ребёнка, заданная в % расчитывается относительно родителя. Поэтому для корректной работы процентной высоты во Flow layout она должна быть выставлена в `100%` для всех возможных родителей (`#root`, `body`, `html`). Или же можно не заморачиваться и использовать другой контекст форматирования — Grid layout, в котором не родитель схлопывается под высоту ребёнка, а наоборот дети растягиваются на высоту родителя

### Платформа

- как узнать, [используется ли на вашем сервере gzip](https://gomakethings.com/how-to-tell-if-your-site-is-gzipped/) или другое сжатие: в девтулзах во вкладке Network смотрим на выполненный запрос, в колонке Size сверху отображается transferred size и пониже actual size; если transferred < actual, значит сжатие есть
