---
title: "Как «прокачать» объект, чтобы он был итерируемым?"
description: "Использовать один из Well-known Symbols, а именно Symbol.iterator"
pubDate: "Feb 20 2023"
heroImage: "/blog-placeholder-lab.jpg"
tags: ["Лаборатория_веб_платформы"]
---

Как «прокачать» объект, чтобы он был итерируемым?

Использовать один из Well-known Symbols, а именно `Symbol.iterator`.

```js
const obj = { a: 1, b: 2, c: 3 };

obj[Symbol.iterator] = function* () {
  for (const key of Object.keys(this)) {
    yield [key, this[key]];
  }
};

for (const [key, value] of obj) {
  console.log(`${key}: ${value}`);
}
```

Также есть вариант для асинхронного итерирования посредством `Symbol.asyncIterator`.

[реф](https://h3manth.com/posts/Well-known-symbols/)
