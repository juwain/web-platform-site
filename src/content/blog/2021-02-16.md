---
title: "Стрелочная функция и new"
description: "Стрелочная функция не может использоваться как конструктор"
pubDate: "Feb 16 2021"
heroImage: "/blog-placeholder-lab.jpg"
tags: ["JS", "Лаборатория_веб_платформы"]
---

Обычная функция `function` может использоваться в качестве конструктора:

```js
function Dog(color) {
  this.color = color;
}

const blackDog = new Dog("black");
```

Стрелочная функция не может использоваться как конструктор. При попытке использования выбросится ошибка TypeError.

```js
const Dog = (color) => {
  this.color = color;
};

const blackDog = new Dog("black");
// TypeError: Dog is not a constructor
```
