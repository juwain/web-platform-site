---
title: Пульс веб-платформы 14.03.2025
description: Новости веб-платформы
pubDate: Mar 14 2025
heroImage: /blog-placeholder-pulse.jpg
tags:
  - Пульс_веб_платформы
---

### Новости

- [вышел Firefox 136](https://www.mozilla.org/en-US/firefox/136.0/releasenotes/), поддержали:
  - [Intl.DurationFormat](https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Intl/DurationFormat)
  - псевдокласс [:open](https://developer.mozilla.org/docs/Web/CSS/:open)
  - [CookieStore API](https://developer.mozilla.org/docs/Web/API/CookieStore)
  - а также значение `plaintext-only` для атрибута [contenteditable](https://developer.mozilla.org/en-US/docs/Web/HTML/Global_attributes/contenteditable)
- [в новый формат конфига ESLint добавили](https://eslint.org/blog/2025/03/flat-config-extends-define-config-global-ignores/) функцию `defineConfig` и вернули `extends`, это и правда удобно, чтобы замиксовать несколько предсобранных конфигов для проекта:

```js
export default defineConfig({
  files: ["**/*.js"],
  extends: ["js/recommended", reactPlugin.configs.flat.recommended],
});
```

- [компилятор TS будет портирован на Go к 7 версии](https://2ality.com/2025/03/typescript-in-go.html) (к ноябрю 2025 ожидается бета-версия) и ускорен в 10 раз:
  - компилятор именно портируется, а не переписывается с нуля (Go хорошо подходит под формат уже имеющегося кода)
  - выигрыш по перфомансу за счёт многопоточности и отсутствия необходимости компиляции нативного кода (в JS упёрлись в потолок)
  - Go-версия может запускаться в браузере через WebAssembly
  - ожидается, что JS- и нативный движок будут существовать долгое время параллельно
- [вышла последняя глава Web Almanac про JS](https://almanac.httparchive.org/en/2024/javascript):
  - в среднем вес JS на сайтах дорос до 500-600kb (часть кода не используется), >20 запросов за JS на странице
  - из бандлеров чаще всего используется webpack, с заметным отставанием Parcel
  - в найденных сорсмапах: TS используется в 6% сайтов, Babel — в 12% из топа 10к мобильных сайтов
  - jQuery используется в 74% сайтов, Swiper — в 15%, React — в 10%, веб-компоненты в 7.8%
  - 67% сайтов включает бесполезно транспилированный JS-код
- выпуск CSS `@function` [откладывается с Chrome 136 на 139 версию](https://developer.chrome.com/blog/delaying-shipping-of-css-functions), так как пока ещё устаканивается спека
- в редакторе Zed [появилась поддержка GUI для работы с git](https://zed.dev/blog/git) (в общем, у Zed и компании есть ещё примерно полгода-год, пока TS в VSCode станет работать супербыстро, и все перестанут с него убегать)

### Проекты

- [endoflife.date](https://endoflife.date) — тут можно посмотреть даты поддержки многих версий либ, программ и фреймворков
- [node-modules.dev](https://node-modules.dev) — `npx node-modules-inspector` в вашем проекте покажет диаграмму зависимостей `node_modules`, их размер и связи
- [same.dev](https://same.dev/) — AI-тула, которая походит по сайту и сгенерит аналог (работает только на простеньких лендосах, но делает их сложно 😕)

### Статьи и демки

#### JS

- если заходит речь про [выбор фронтового фреймворка](https://allenpike.com/2025/javascript-fatigue-ssr), лучше выбирать что-то поскучнее и стабильнее, правда стабильных и скучных фреймворков маловато (Next.js вендорлочит, Astro про контент, React Router всё ещё в пучине перерождений)
- недавняя поддержка TS в Node [вызывает улыбку на лице Ryan Dahl](https://deno.com/blog/typescript-in-node-vs-deno): в Deno TS не только тайпстрипается, но ещё и форматируется, компилируется, линтится и генерит документацию
- [паттерн возвращения](https://advancedweb.hu/shorts/javascript-best-practice-use-return-await/) `return await func()` хорош тем, что дождётся выполнения кода, и если нужно поймать ошибку, то выбросит её, пока не произошёл выход из функции:

```js
const helloWorld = async () => {
  try {
    return await asyncHello("World");
  } catch (e) {
    return "Whops";
  }
};
```

- а что если [«билдить» архив с вашими md-файлами просто в HTML](https://dev.to/fedia/15kb-single-file-wiki-46a1)? Навигацию можно сделать на CSS-селекторах `:target`, HTML-файл скачивается один раз (offline-first), не нужен JS
- URL — идеальное место, чтобы [хранить сериализуемое состояние приложения](https://iamsahaj.xyz/blog/react-state-in-the-url/), но есть ограничение: на 60-80к символов в URL браузер может загнуться
- [база про перфоманс в React](https://cekrem.github.io/posts/beyond-react-memo-smarter-performance-optimization/): отделяйте изменяющийся стейт рядом с тяжёлыми компонентами в отдельные компоненты для избежания ререндера
- явное лучше неявного на примере [выделения данных или конфигов в коде](https://www.trevorlasn.com/blog/explicit-is-better-than-implicit) в отдельные переменные
- [API стейт-менеджера Jotai](https://newsletter.daishikato.com/p/how-the-jotai-store-api-is-inspired-by-the-weakmap-api) вдохновлено `WeakMap` + добавлена возможность подписки на мапу

#### CSS

- если в вашем приложении есть отстраивание побочных цветов от основных цветов приложения (обводки, тени, градиенты, разделители, состояния UI-элементов, оттенки), то [в деле поможет relative color](https://ishadeed.com/article/css-relative-colors/)
- в Chrome 135 нас ждёт появление двух CSS-функций [sign](https://developer.mozilla.org/en-US/docs/Web/CSS/sign) и [abs](https://developer.mozilla.org/en-US/docs/Web/CSS/abs): для получения знака выражения (-1 или 1) и модуля числа
- [в канарейке Chrome доступны](https://nerdy.dev/sibling-index) функции [`sibling-index()`](https://drafts.csswg.org/css-values-5/#tree-counting) и [`sibling-count()`](https://drafts.csswg.org/css-values-5/#tree-counting) для получения индекса элемента и числа всех элементов на одном уровне в DOM
- полупрозрачное [«визуально спокойное» подчёркивание ссылок](https://frontendmasters.com/blog/chilled-out-text-underlines/) `text-decoration-color: color-mix(in srgb, currentColor, transparent 75%)`
- существует специальное свойство [для анимации движения элемента по SVG-пути](https://css-tip.com/infinite-logos-animation/) — это `offset`
- с помощью `content-visibility` и `contain` [можно указать браузеру](https://www.debugbear.com/blog/content-visibility-api) не стилизовать, не строить лейаут или не отрисовывать определённый кусок интерфейса для улучшения перфоманса (наколеночная виртуализация)

#### HTML

- [интересная идея](https://muffinman.io/blog/image-comparison-slider/) использовать для визуального «сравнивателя» картинок `<input type="range">` в качестве управляющего элемента для задания кастомного свойства с шириной картинки

### Платформа

- статья, где рассказывается про миграцию с одного тестового движка на другой (но это неважно), [интересны три предлагаемых подхода к миграции](https://open.nytimes.com/how-the-new-york-times-systematically-migrated-from-enzyme-into-react-testing-library-b3ea538d001c): 1) бульдозер — мигрируем все файлы одним скопом, 2) консенсус — разделяем ответственность за каждый файл между конкретными людьми, 3) по частям — берём самый малозначительный файл, мигрируем, идём к следующему, повторяем, пока файлы не закончатся
- `git switch -` или `git checkout -` (для староверов) [переключает на прошлую ветку](https://joshtronic.com/2025/03/09/switch-to-previous-checked-out-git-branch/) в гите
